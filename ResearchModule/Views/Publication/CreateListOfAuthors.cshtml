@using ResearchModule.Components.Models.Card
@using ResearchModule.Components
@using Microsoft.AspNetCore.Html
@inject ResearchModule.Service.PublicationService PublicationService
@model long

@*@Html.Card("Author").Collapse().Header("Список авторов").Row((Card a) =>
{
    a.For()
    .Content(Render(CustomCreate()))
        .Id("countResult");
    return a;
}).Render()*@

<div class="panel panel-default">
   
    <div class="panel-heading">Список авторов</div>
    @*<div class="panel-body">
        <p>Описание...</p>
    </div>*@
    <table class="table">
        <thead>
            <tr>
                <th></th>
                <th>Фамилия</th>
                <th>Имя</th>
                <th>Отчество</th>
                <th>Дата рождения</th>
                <th data-toggle="tooltip" title="Определение веса каждого из авторов">Вес *<span id="count_Authors"></span></th>
            </tr>
        </thead>
        <tbody id="createResult"></tbody>
        <tbody id="searchResult"></tbody>
        @{
            if (Model != 0)
            {
                <tbody id="getResult">@Html.Partial("../Author/Search", PublicationService.GetAuthorsByPublication(Model))</tbody>
            }
        }
    </table>
    
</div>

